# External-observer findings — Ember

> Written by `/critique` after walking the live site as a
> fresh-eyes visitor. Drained by `/iterate`.

## Pending

(none yet — but the site IS live at https://ember-rust-sigma.vercel.app.
`/critique` has never run. The next local `/march` — or a direct
`/critique` — should walk the deployed site as a fresh-eyes visitor.
The cloud loop skips `/critique` by design, so this only happens
when the user runs the loop locally.)

## Done

(none yet)
