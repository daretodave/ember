{
  "name": "@ember/e2e",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "e2e:install": "playwright install chromium --with-deps"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0"
  }
}
